# Data_Networks_DHCP
Data Networks - DHCP: This project demonstrates the configuration of a centralized DHCP server on a Cisco router to provide dynamic IP addressing across multiple subnets. It includes VLSM subnetting, OSPF routing for full connectivity, and the use of IP helper addresses for DHCP relay.
